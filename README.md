# Assignment-04


### 1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
## ans: getElementById is only catch an unique id in the desire html file, getElementsByClassName it's can catch all element under a class, querySelector is something like getElementById but it can go deep dive by traversing which css selector is provide and querySelectorAll it's catch any of specific all css selector 


### 2. How do you create and insert a new element into the DOM?
## ans: for create a new method is document.createElement() and insert method is parentElement.append(childElement)

### 3. What is Event Bubbling? And how does it work?

## ans: Event bubbling is javascript behavior where is work first go deep dive and when after it come it's initial position the following way first it's come it's last node's parent node then it's parent node and finally it's come initial node which is the root parent of all child node.

### 4. What is Event Delegation in JavaScript? Why is it useful?
## ans: 

### 5. What is the difference between preventDefault() and stopPropagation() methods?
## ans: 